export type ServiceFormData = {
  specialtyId: string;
  name: string;
  description: string;
  price: string;
  durationMinutes: string;
  active: boolean;
  onlineBooking: boolean;
  minimumAdvanceHours: string;
};