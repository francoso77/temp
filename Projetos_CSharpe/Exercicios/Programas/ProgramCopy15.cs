using System;
using System.Globalization;

namespace ExerciciosCopy15
{
    class ProgramCopy15
    {
        static void MainCopy15(string[] args)
        {
            
            /*
            Console.Write("Informe a quantidade de quartos: ");
            int n = int.Parse(Console.ReadLine());

            Pensao[] pensao = new Pensao[10];

            for (int i = 0; i < n; i++)
            {
                Console.Write("Informe o name: ");
                string name = Console.ReadLine();
                Console.Write("Informe o email: ");
                string email = Console.ReadLine();
                Console.Write("Informe o número do quarto: ");
                int quarto = int.Parse(Console.ReadLine(), CultureInfo.InvariantCulture);

               // Aqui sem Constructor
               // pensao[quarto] = new Pensao { Name = name, Email = email };
                
                // aqui com constructor
                pensao[quarto] = new Pensao ( name,  email );
            
            }

            Console.WriteLine("Quartos alugados:");
            for (int i = 0; i < 10; i++) {
                if (pensao[i] != null){
                    Console.WriteLine(i + ", " + pensao[i]);
                }
             }
             */
        }
    }
}
