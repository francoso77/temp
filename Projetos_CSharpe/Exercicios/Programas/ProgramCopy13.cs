using System;
using System.Globalization;

namespace ExerciciosCopy13
{
    class ProgramCopy13
    {
        static void MainCopy13(string[] args)
        {

            /*
            //Usando  do tipo struct
            Console.Write("Informe o tamanho do vetor: ");
            int n = int.Parse(Console.ReadLine());

            double soma = 0.0;
            double media = 0.0;
            double[] alturas = new double[n];

            for (int i = 0; i < n; i++){

                Console.Write("Informe a altura da posição " + i + " do  vetor: ");
                alturas[i] = double.Parse(Console.ReadLine(), CultureInfo.InvariantCulture);
                soma += alturas[i];
            }

            media = soma / n;
            Console.WriteLine("A média das alturas é: " + media.ToString("F2", CultureInfo.InvariantCulture));

            */
        }
    }
}
