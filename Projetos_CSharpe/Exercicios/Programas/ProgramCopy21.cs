using System;
using System.Collections.Generic;
using System.Globalization;

namespace ExerciciosCopy21
{
    class ProgramCopy21
    {
        static void MainCopy21(string[] args)
        {
            /*
            int negativos = 0;
            Console.Write("Entre com a quantidade de linhas: ");
            int l = int.Parse(Console.ReadLine());

            Console.Write("Entre com a quantidade de colunas: ");
            int c = int.Parse(Console.ReadLine());

            int[,] numeros = new int[l,c];

            for (int i = 0; i < l; i++)
            {
                Console.Write("Entre com os números da linha #" + i + ". Ex (1 32 -5): ");
                string[] valores = Console.ReadLine().Split(' ');

                for (int j = 0; j < c; j++) {
                    numeros[i,j] = int.Parse(valores[j]);
                }
            }

            Console.WriteLine();
            Console.Write("Encontre o número: ");
            int searchNumero = int.Parse(Console.ReadLine());

            for (int i = 0;i < l; i++)
            {
                for (int j = 0; j < c; j++)
                {
                    if (numeros[i, j] == searchNumero)
                    {
                        Console.WriteLine("Posição(" + i + ", " + j + "):");
                                                
                        if (j - 1 >=  0)
                        {
                            Console.WriteLine("O número a esquerda é: " + numeros[i, j - 1]);
                        }
                        if (j + 1 < c) 
                        {
                            Console.WriteLine("O número a direita é: " + numeros[i, j + 1]); 
                        }
                        if (i -1 >= 0) 
                        {
                            Console.WriteLine("O número acima é: " + numeros[i - 1, j]); 
                        }
                        if (i < l) 
                        {
                            Console.WriteLine("O número a baixo é: " + numeros[i + 1, j]); 
                        }
                        
                    }
                }
            }
            */
        }
    }
}
