using System;
using System.Collections.Generic;
using System.Globalization;

namespace ExerciciosCopy20
{
    class ProgramCopy20
    {
        static void MainCopy20(string[] args)
        {

            /*
            int negativos = 0;
            Console.Write("Entre com o tamanho da matriz: ");
            int n = int.Parse(Console.ReadLine());

            int[,] numeros = new int[n,n];

            for (int i = 0; i < n; i++)
            {
                Console.Write("Entre com os números dessa linha #" + i + ". Ex (1 32 -5): ");
                string[] valores = Console.ReadLine().Split(' ');

                for (int j = 0; j < n; j++) {
                    numeros[i,j] = int.Parse(valores[j]);
                    if (int.Parse(valores[j]) < 0)
                    {
                        negativos++;
                    }
                }
            }

            Console.WriteLine("Main Diagonal: ");
            for (int i = 0;i < n; i++)
            {
                Console.Write(numeros[i, i] + " ");
            }
            Console.WriteLine();
            Console.Write("A quantidade de números negativos é: " + negativos);

            */
        }
    }
}
