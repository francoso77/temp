using System;
using System.Globalization;

namespace ExerciciosCopy14
{
    class ProgramCopy14
    {
        static void MainCopy14(string[] args)
        {

            /*
            //Usando  do tipo struct
            Console.Write("Informe o tamanho do vetor: ");
            int n = int.Parse(Console.ReadLine());

            double soma = 0.0;
            double media = 0.0;
            Product[] produtos = new Product[n];

            for (int i = 0; i < n; i++){

                Console.Write("Informe o name do produto " + i + " :");
                string name = Console.ReadLine();
                Console.Write("Informe o price do produto " + i + " :");
                double price = double.Parse(Console.ReadLine(), CultureInfo.InvariantCulture);

                produtos[i] = new Product { Name = name, Price = price};
                soma += produtos[i].Price;
            }

            media = soma / n;
            Console.WriteLine("A média dos produtos é: " + media.ToString("F2", CultureInfo.InvariantCulture));

            */
        }
    }
}
